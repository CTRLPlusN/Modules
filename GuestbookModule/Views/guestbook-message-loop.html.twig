<h2>Les Messages</h2>
{% for message in messages %}
    <p class="w3-center w3-xlarge">
        <em>
            {{ message.content }}
        </em>
        <br/>
        <span class="w3-medium w3-text-grey">
            écrit par <strong>{{ message.author }}</strong> le {{ message.created|localizeddate('full', 'none') }}
        </span>
    </p>
    <hr/>
{% endfor %}