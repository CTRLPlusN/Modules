{% extends '@admin/base.html.twig' %}

{% block body %}
    <div class="{{ css_ctrl_bar }}">
        {{ btn.add('admin_album_add') }}
    </div>
    <section class="{{ css_section }}">
        {{ layout.header("Liste des albums") }}
        {% import '@picture/admin-macro-galery.html.twig' as grid %}
        {{ grid.galery_album(albums) }}
    </section>
{% endblock %}